import React from "react";

import BookButton from "./BookButton/BookButton";

const Book = props => (
  <div>
    <BookButton />
    <div>Book</div>
    <BookButton />
  </div>
);

export default Book;
